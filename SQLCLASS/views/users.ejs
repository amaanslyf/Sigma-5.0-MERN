<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>

<body>
    <h1>Below is the list of all users in database</h1>
    <table border="1" cellpadding="10" cellspacing="0">
        <tr>
            <th>id</th>
            <th>username</th>
            <th>email</th>
            <th>edit action</th>
        </tr>
        <% for (let i=0; i < result.length; i++) { %>
            <tr>
                <td>
                    <%= result[i].id %>
                </td>
                <td>
                    <%= result[i].username %>
                </td>
                <td>
                    <%= result[i].email %>
                </td>
                <td>
                    <form action="/user/<%= result[i].id %>/edit" method="get">
                        <button>Edit username</button>
                    </form>
                </td>
            </tr>

            <% } %>
    </table>

</body>

</html>